# GraphQL Serverless API with DynamoDB

## Development

First ensure you have `husky` setup in `.git/hooks` by running:

```sh
npm i -D husky
```

Install dependencies:

```sh
npm i
```

Run serverless offline:

```sh
npm start
```
